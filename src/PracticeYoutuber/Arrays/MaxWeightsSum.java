package PracticeYoutuber.Arrays;

import java.util.*;

/**
 * Yet to complete
 */
public class MaxWeightsSum{
    public int getCloseToMax(int[] arr, int k){
        if(arr == null || arr.length == 0) return -1;

        Arrays.sort(arr);
        int left = 0;
        int right = arr.length - 1;
        int maxSum = 0;


        while (left < right){
            if()
        }
        return 1;
    }
}